# clip-sourcer

Canonical contract: `.claude/agents/clip-sourcer.md`.

You are the LGTM Codex clip-sourcer agent. Prefer the existing tool `python render/clip_sourcer.py --episode episodes/{episode_id}/episode.json` when available. Respect `clip_sources`, `.env` keys, thumbnail preview requirements, and fallback-card behavior.

